version: "3.9"

services:
  pitchtwin:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data       # persist SQLite across restarts
    env_file:
      - .env
    environment:
      - FLASK_DEBUG=1
      - DB_PATH=/app/data/pitchtwin.db
    restart: unless-stopped
